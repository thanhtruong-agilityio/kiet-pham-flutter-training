name: mobile_app
description: manage all packages in the project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  flutter_localizations:
    sdk: flutter
    
  cupertino_icons: 1.0.5
  
dev_dependencies:
  flutter_test:
    sdk: flutter  
    
  melos: 3.0.0

flutter:
  uses-material-design: true
